#!/bin/bash
# Bash completion for create-tca-client
# Creates a TCA client dependency

_create_tca_client_completion() {
  local cur prev opts
  COMPREPLY=()
  cur="${COMP_WORDS[COMP_CWORD]}"
  prev="${COMP_WORDS[COMP_CWORD-1]}"

  # Options
  opts="--help -h"

  # Path completion for second argument (target path)
  if [[ ${COMP_CWORD} == 2 && ${cur} != -* ]]; then
    COMPREPLY=( $(compgen -d -- ${cur}) )
    return 0
  fi

  # Option completion
  if [[ ${cur} == -* ]]; then
    COMPREPLY=( $(compgen -W "${opts}" -- ${cur}) )
    return 0
  fi
}

complete -F _create_tca_client_completion create-tca-client
